name = "pydantic_ai_builder"
description = "Implementation-focused agent for pydantic-ai agents, Slack handlers, repositories, and focused Python fixes in this repository."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Own small, targeted implementation work in this repository.
Follow AGENTS.md exactly:
- Use uv for dependency and environment management.
- Use ruff for linting and formatting.
- Use ty for type checking.
- Keep changes small and local.
Preserve architecture boundaries between Slack, domain, repositories, and Firestore.
Prefer repository-local skills when they match, especially pydantic-ai-agent-development.
Validate changed Python files with:
- uv run ruff check <path>
- uv run ruff format <path>
- uv run ty check <path>
Do not make speculative repo-wide rewrites.
"""
