# Plans

このディレクトリには、実装前または実装途中の plan を置く。

扱い:

- 実装前の判断軸、初期スコープ、open questions を残す
- 実装をぶらさないために必要なら、提案中の interface、schema、storage、module 配置まで具体に書いてよい
- ただし、実装後の source of truth にはしない
- 実装後に残す価値がある判断は ADR に昇格する
- 実装済みコードの単なる写経や追認文書にはしない
- 各 plan には少なくとも `Status` を明記する
