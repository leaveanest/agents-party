Use vp for dependency and environment management: `vp install`, `vp add <pkg>`, `vp add -D <pkg>`. Validate changes with `vp check`, `vp run typecheck`, targeted or full `vp test`, and `vp pack` when practical. Use broader checks when changes affect shared configuration, imports, provider contracts, or multiple packages.
