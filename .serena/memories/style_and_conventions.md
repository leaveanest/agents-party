Keep changes small and local. Use vp for dependency management, formatting/linting, type checking, tests, and packaging. Keep Slack SDK usage inside src/slack, provider-specific SDK usage inside src/providers, PostgreSQL SDK usage inside src/infrastructure/postgres, and domain models independent from Slack, AI SDK, and database SDK details.
